<p id="notice"><%= notice %></p>

<table>
  <thead>
    <tr>
      <th colspan="3"></th>
    </tr>
  </thead>

  <tbody>
    <% @interventions.id do |intervention| %>
      <tr>
        <td>
        <%= intervention.id %> 
        <td>
        <%= intervention.customer.business_name %>
        <td>
        <%= intervention.building.building_name %> 
        <td> 
        <%= link_to 'Destroy', intervention_path(intervention), method: :delete, data: { confirm: 'Are you sure?' } %></td>
      </tr>
    <% end %>
  </tbody>
</table>

<%= link_to 'Back', interventions_path %>
